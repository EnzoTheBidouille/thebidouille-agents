{
  "name": "cohorte-dashboard-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "React (Vite) front-end for the cohorte pipeline dashboard. Built to ../dist and served by the dependency-free node server.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
